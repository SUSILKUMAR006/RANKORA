import { Camera, ShieldCheck } from 'lucide-react'
import Card from '../../common/Card.jsx'
function VerificationRequirement({ required }) { return <Card variant="standard" className={required ? 'border-amber-300/20 bg-amber-400/[0.04]' : 'border-white/10'}><div className="flex gap-3"><span className={`grid h-9 w-9 shrink-0 place-items-center rounded-lg ${required ? 'bg-amber-300/10 text-amber-200' : 'bg-emerald-300/10 text-emerald-200'}`}>{required ? <Camera size={18} /> : <ShieldCheck size={18} />}</span><div><p className={`label-caps ${required ? 'text-amber-200/70' : 'text-emerald-200/70'}`}>{required ? 'VERIFICATION REQUIRED' : 'NO VERIFICATION REQUIRED'}</p><p className="mt-2 text-sm leading-6 text-slate-400">{required ? 'This quest requires photo evidence before it can be completed.' : 'This objective can be completed without additional evidence.'}</p>{required && <p className="mt-3 font-mono text-xs text-amber-200">PHOTO REQUIRED</p>}</div></div></Card> }
export default VerificationRequirement
